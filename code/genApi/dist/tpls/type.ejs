<% deps.forEach(function (dep){%>
    export interface ListRouteBusResData {
    list?: TpRoutBusVO[];
    pageNum?: number;
    pageSize?: number;
    total?: number;
    totalPage?: number;
    }
<%})%>
